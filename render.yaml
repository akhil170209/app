
services:
  - type: web
    name: my-python-app
    env: python
    region: oregon
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python app.py
    pythonVersion: 3.11
